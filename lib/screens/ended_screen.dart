import 'package:flutter/material.dart';

class EndedScreen extends StatelessWidget
{
  @override
  Widget build(BuildContext context)
  {
    return Scaffold
    (
      body: Center(child: Text('Vendas Online Encerradas', style: TextStyle(fontSize: 24))),
    );
  }
}
